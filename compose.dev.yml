services:
    final-hour:
        build:
            context: .
            target: final-hour-dev
        restart: unless-stopped
        environment:
            - FH_NO_DISCORD=1
        volumes:
            - ./:/home/node/app
            - ./maps:/home/node/app/maps
        ports:
            - 13000:13000/udp
